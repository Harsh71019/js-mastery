MONGODB_URI=
PORT=3001
NODE_ENV=development

# Comma-separated list of allowed CORS origins (leave empty to allow all)
ALLOWED_ORIGINS=http://localhost:5173

# Bearer token required on all /api/admin/* requests (leave empty to disable auth)
ADMIN_TOKEN=
